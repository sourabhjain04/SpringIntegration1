spring:
  application:
    name: ORDER-SERVICE
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb

server:
  port: 9192

  
management:
  tracing:
    sampling:
      probability : 1.0